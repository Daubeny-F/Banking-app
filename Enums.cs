namespace BankingSystem
{
    public enum BankLocation
    {
        TM, AR, B, CT, IS, CJ, TL, BR, BV, DB
    }

    public enum BankCountry
    {
        RO, HU, DE, GB, FR, IT, ES, PL
    }

    public enum AccountType
    {
        Person,
        Company,
        Special
    }

    public enum AccountCurrency
    {
        RON, EUR, USD, GBP
    }
}
